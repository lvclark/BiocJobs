% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tes.R
\name{tesTask}
\alias{tesTask}
\title{Generate a GA4GH TES task from a job}
\usage{
tesTask(
  job,
  inputs = character(),
  outputs = character(),
  options = list(),
  image = NULL,
  workdir = "/tmp/biocjob",
  tags = character(),
  bootstrap = NULL
)
}
\arguments{
\item{job}{A \code{BiocJob} object, or path to a job YAML file.}

\item{inputs}{Named character vector of input URLs (e.g.
\code{c(counts = "s3://bucket/counts.tsv")}).}

\item{outputs}{Named character vector of destination URLs for outputs.}

\item{options}{Named list of option values; unspecified options fall back
to their declared defaults.}

\item{image}{Container image; defaults to the job's \code{container} field,
then to the current Bioconductor docker image.}

\item{workdir}{Working directory inside the container.}

\item{tags}{Additional tags (named character) merged into the task tags.}

\item{bootstrap}{Prepend an executor that installs the host package,
BiocJobs, and the job's declared \code{depends} via \code{BiocManager} if they
are missing from the image.  Default (\code{NULL}): enabled exactly when the
image is the generic Bioconductor docker image (which ships no analysis
packages); disabled for purpose-built images named via \verb{container:} or
\code{image}.}
}
\value{
A list representing the TES task, class \code{"TesTask"}.
}
\description{
Produces a TES v1.1 \code{tesTask} structure ready to POST to a TES
implementation's \verb{/tasks} endpoint (Funnel, TESK, ...).  Any input or
output whose URL is not supplied, and any required option without a
value, is emitted as a \code{{{...}}} placeholder, making the result a
submission template.
}
\seealso{
\code{\link[=writeTesTask]{writeTesTask()}}
}
