% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/command.R
\name{runJob}
\alias{runJob}
\title{Run a job locally}
\usage{
runJob(
  job,
  params = list(),
  workdir = tempfile("biocjob_"),
  rscript = "Rscript"
)
}
\arguments{
\item{job}{A \code{BiocJob} object, or path to a job YAML file.}

\item{params}{Named list of parameter values (see \code{jobCommand()}).}

\item{workdir}{Working directory for the run (created if needed);
defaults to a fresh temporary directory.  Relative output paths land
here.}

\item{rscript}{Path or name of the \code{Rscript} executable.}
}
\value{
Invisibly, a list with \code{status} (exit code), \code{workdir}, and
\code{outputs} (named vector of absolute paths to declared outputs).
}
\description{
Runs a job in a fresh \code{Rscript} process, without requiring the host
package's jobs to be installed: the specification path is passed to the
child process via the \code{BIOCJOBS_SPEC} environment variable, so a job can
be exercised straight from a package source checkout.
}
